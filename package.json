{
  "name": "billing-software-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Enterprise SaaS Billing Software Monorepo",
  "scripts": {
    "build": "pnpm --recursive run build",
    "dev": "pnpm --recursive --parallel run dev",
    "test": "pnpm --recursive run test",
    "lint": "eslint"
  },
  "devDependencies": {
    "eslint": "^9",
    "typescript": "^5.3.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "prisma",
      "@prisma/engines",
      "bcrypt",
      "@nestjs/core",
      "@nestjs/common"
    ]
  }
}
