// Entry file for shared monorepo components library (@saas/ui)
// Export reusable UI sub-components here

export const UI_LIBRARY_VERSION = '1.0.0';

export function getSharedThemeStyles() {
  return {
    primaryColor: '#6366f1',
    borderRadius: '0.5rem',
  };
}
